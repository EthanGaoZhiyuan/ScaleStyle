# Phase 3: Vision & Multimodal Requirements
# These are additional/highlighted requirements for CLIP-based image embedding
# Most are already in main requirements.txt but listed here for clarity

# Core vision dependencies
sentence-transformers>=5.2.0  # CLIP model interface (already in main)
torch>=2.9.0                   # PyTorch backend (already in main)
pillow>=12.0.0                 # Image loading (already in main)
transformers>=4.57.0           # HuggingFace transformers (already in main)

# Additional dependencies (if needed for specific CLIP implementations)
# open_clip_torch>=2.26.0      # [Optional] OpenCLIP implementation
# timm>=1.0.0                  # [Optional] For vision transformer variants

# Note: All dependencies above are already satisfied by main requirements.txt
# This file serves as documentation for Phase 3 vision module dependencies
